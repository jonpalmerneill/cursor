# Supabase (copy these to config.js for the frontend; anon key is safe in client)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# OpenAI key is stored in Supabase Edge Function secrets, not here.
# Set it in Supabase Dashboard: Project Settings → Edge Functions → Secrets → OPENAI_API_KEY
